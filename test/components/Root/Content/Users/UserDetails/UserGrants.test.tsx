import React from 'react';

describe('UserGrants', () => {
	describe('rendering', () => {
		it('renders with clients', () => {
			throw new Error();
		});

		it('renders without clients', () => {
			throw new Error();
		});

		it('renders without clients with add button disabled', () => {
			throw new Error();
		});
	});

	describe('behavior', () => {
		it('add a client, with no more clients available to add', () => {
			throw new Error();
		});

		it('go to a client', () => {
			throw new Error();
		});

		it('select a client, but no roles available', () => {
			throw new Error();
		});

		it('select a client and add a role', () => {
			throw new Error();
		});

		it('select a client and remove a role', () => {
			throw new Error();
		});

		it('cancel add client dialog', () => {
			throw new Error();
		});

		it('cancel add role dialog', () => {
			throw new Error();
		});

		it('cancel remove client dialog', () => {
			throw new Error();
		});

		it('cancel remove role dialog', () => {
			throw new Error();
		});
	});
});
