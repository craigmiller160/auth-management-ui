import React from 'react';
import Dialog from '@material-ui/core/Dialog';

const ConfirmDialog = () => {
    return (
        <Dialog open={ true }>
            <h1>Hello World</h1>
        </Dialog>
    );
};

export default ConfirmDialog;
